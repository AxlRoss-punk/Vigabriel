// URL base de la API REST. Se usa localhost porque este archivo se ejecuta
// en el NAVEGADOR del usuario (fuera de la red interna de Docker), por lo que
// debe usar el puerto publicado por el contenedor "api" en el host.
window.API_URL = 'http://localhost:4000/api';
